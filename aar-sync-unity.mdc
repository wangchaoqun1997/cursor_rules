---
description: 改动涉及 Android 原生/AAR 时，任务结束前须重打 AAR 并覆盖到 Unity Plugins（VR局域网控）
alwaysApply: true
---

# Android AAR 同步 Unity（VR局域网控）

凡改动会进入 **APK 的 AAR / `Plugins/Android` 原生内容**，收尾阶段必须 **构建 + 覆盖**，不能只改源码就结束。收尾安装提醒见 `build-deploy-reminder.mdc`。

## 本仓库当前

### 不必重打 AAR

- 仅改 **`docs/**`**、**`.cursor/**`**、纯 C#（如 `aarForAllSlaves/UnityPackage` 无原生改动）、`masterVrLanControl` 且未改会进 AAR 的源码时 → **不必**打 PicoBoot / SlaveExit AAR（Master 是独立 Gradle APK，按 Master 工程打包）。
- 纯文档 / 规则 → 不必 AAR。

### 触及以下任一时 → 必须重打并覆盖

| 触及内容 | 典型路径 | 动作 |
|----------|----------|------|
| 自启 / lanagent | `slaveBootAutoStart/PicoBootAutostart/` | `slaveBootAutoStart/build.ps1` → 覆盖 `Assets/Plugins/Android/PicoBootAutostart.aar` |
| 业务 EXIT / 会话 AAR | `aarForAllSlaves/**/Plugins/Android/*.aar` 及对应 Gradle | 按该子工程脚本 `assembleRelease` 并覆盖到 UPM/Plugins |
| 修改 AAR 内资源 | `src/main/assets`、`res/`、JNI `.so` | 同上，禁止只改源目录不复制产物 |
| AndroidManifest / 权限 | 插件自带 Manifest 合并 | 改完后打 **gameslauncher / 业务** Android 包并在真机验证 |

具体命令以子工程内 **`build.ps1` / `copyunity.bat` / `gradlew.bat`** 或 README 为准；助手在任务中 **自行执行** 并回复目标路径。

### 本仓库已接入

| 项 | 值 |
|----|-----|
| PicoBootAutostart | 源码 `slaveBootAutoStart/`；`build.ps1`；目标 `Assets/Plugins/Android/PicoBootAutostart.aar` |
| 业务 UPM | `aarForAllSlaves/UnityPackage`（含可选 Android AAR） |

### 其它

- **PICO Unity Integration SDK** 等若升级，须在文档记版本，并在 **gameslauncher（头显）** 构建上验证。
- 若日后新增 Gradle 子工程，在本文件下 **追加一节**：源目录、构建命令、目标 `Plugins/Android` 路径。

## 交给助手的执行要求

- 自行运行构建与复制；回复中说明已执行的命令、产物路径，或说明 **本次为何不必打 AAR**。
