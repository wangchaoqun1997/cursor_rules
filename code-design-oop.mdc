---
description: 代码须遵循面向对象设计，禁止巨型类（除非有充分理由）
alwaysApply: true
---

# 面向对象与类规模约定

编写或重构 **任何 C# / Unity 代码** 时须遵守：

## 设计原则

- **面向对象**：职责清晰、高内聚低耦合；优先 **组合优于继承**，用 **接口/抽象类** 隔离平台与模块（如 `IVideoPlayer`、`IBroadcastTransport`）。
- **单一职责（SRP）**：一个类只做一类事（例如：网络传输、同步算法、UI 绑定、片源校验分开）。
- **开闭原则**：对扩展开放（新平台、新指令类型），对修改封闭（少改已有核心类）。
- **依赖倒置**：业务层依赖抽象，不直接绑死 LiteNetLib、AVPro、Pico SDK 等具体实现。

## 类规模（硬约束）

| 级别 | 行数（约，含空行与注释） | 要求 |
|------|--------------------------|------|
| 推荐 | **≤ 300** | 常态保持可读、可测 |
| 警戒 | **300～500** | 应评估拆分（partial 仅作辅助，不能代替设计拆分） |
| 禁止 | **> 500** | 必须拆分为多个类/文件，并在提交说明中写清理由 |
| 例外 | **> 500** | **仅当实在必要**（如生成代码、第三方封装薄壳）；须在 PR/提交中注明原因，并尽量用 partial 按子职责分文件 |

**不要**出现「一个类几千行」的上帝类（God Class），例如把所有网络、同步、UI、配置塞进 `MasterController`。

## 推荐拆分方式（播控工程）

```
Broadcast.Core/        → 协议 DTO、会话状态机
Broadcast.Network/     → LiteNetLib 封装，无 Unity 依赖
Broadcast.Sync/        → TimelineAuthority、纠偏策略、授时
Broadcast.Master/      → 中控 UI、放映流程编排（薄）
Broadcast.Slave/        → VR 播放入口、纠偏执行（薄）
Broadcast.Platform/    → 各平台 IVideoPlayer / IAudioClock 实现
```

- **编排类**（Bootstrap、Coordinator）只负责组装与调用，**不写**大段算法与协议细节。
- **数据类**（Config、Manifest、Packet）与 **行为类**（Transport、SyncController）分离。

## 新增代码前自检

1. 这个类是否超过一个修改理由？若是，拆。
2. 能否用接口 + 2～3 个实现类替代分支堆砌？
3. 测试或 Mock 是否只需替换接口即可？若否，说明耦合过重，应重构。
