---
description: 用户说「提交」时 git commit 消息格式（提问/原因/总结）
alwaysApply: true
---

# Git 提交消息（中文）

当用户在对话中说 **提交**、`git 提交`、commit 等，且意图为对本仓库做 commit 时：

1. **先确认暂存范围**：默认只提交与当前任务相关的文件；不要把 Oculus、UserSettings、无关 Assets 测试资源等一并 `git add -A`，除非用户明确要求「全量提交」。
2. **提交信息必须包含三块**（可再补一行简短英文/中文标题作第一行 subject）：

   - **【提问】** 用户原话或等价复述：要解决什么问题、约束是什么。
   - **【原因】** 根因或设计背景（技术层面为何如此改）。
   - **【总结】** 本次改动做了什么（文件/行为层面的结论，1～3 句）。

3. **第一行 subject** 建议简短，例如：`fix: FloorPlanGrid 仅对本组件材质写 Tint`（便于 `git log --oneline`）。

4. 多块正文用空行分隔；避免无意义的 emoji。
