---
description: Java→C# 必须用 AndroidJavaProxy 等 JNI 回调；禁止 UnityPlayer.UnitySendMessage
globs: slaveBootAutoStart/**/*,aarForAllSlaves/**/*
alwaysApply: true
---

# Android Java → Unity C#：禁止 UnitySendMessage

## 硬约束

凡 **Java / Kotlin（AAR）回调 Unity C#**：

- **禁止**使用 `UnityPlayer.UnitySendMessage`（含 `UnitySendMessage` 字符串反射调 MonoBehaviour）。
- **必须**使用 **JNI 回调接口**，首选 **`AndroidJavaProxy`**（C# 实现 Java 接口，注册给 AAR）。

`UnitySendMessage` 依赖 GameObject 名、方法名字符串、主线程派发，难测、易丢消息、与 IL2CPP 裁剪/重构不友好；本工程 **一律不用**。

## 推荐模式（本仓库示例：会话 Handler）

### Java：定义回调接口

```java
// com.wangcq327.vrboardcast.slaveexit.ISlaveSessionHandler
public interface ISlaveSessionHandler {
    void onSessionCommand(String op, String payloadJson);
}
```

### C#：AndroidJavaProxy 实现并传入 Java

```csharp
sealed class SessionHandlerProxy : AndroidJavaProxy
{
    public SessionHandlerProxy()
        : base("com.wangcq327.vrboardcast.slaveexit.ISlaveSessionHandler") { }

    // ReSharper disable once UnusedMember.Local — JNI
    void onSessionCommand(string op, string payloadJson) { /* 入队主线程后再处理 */ }
}

// handlers.CallStatic("set", new SessionHandlerProxy());
```

### 主线程

Java 回调可能在 **非 Unity 主线程**。在 Proxy 内 **不要**直接调 Unity API；用主线程队列 **Enqueue** 后再更新状态。

## C# → Java

仍用 `AndroidJavaObject` / `AndroidJavaClass.Call`；与 Java→C# 方向独立，不受本节禁止项影响。

## 允许的其他 Java→C# 方式

| 方式 | 适用 |
|------|------|
| `AndroidJavaProxy` 实现 Java 接口 | **首选** |
| C# **主动轮询** Java getter | 低频快照 |
| 共享内存 / 文件 / Socket | 非本项目会话首选 |

## 相关

- AAR 同步纪律：[aar-sync-unity.mdc](./aar-sync-unity.mdc)
- 业务会话 / EXIT：[aarForAllSlaves/docs/](../../../aarForAllSlaves/docs/)
