---
description: Java→C# 必须用 AndroidJavaProxy 等 JNI 回调；禁止 UnityPlayer.UnitySendMessage
globs: AndroidProject_SlaveDownload/**/*,Assets/Broadcast/Slave/ContentSync/**/*.cs,Assets/Broadcast/Slave/Plugins/**/*
alwaysApply: true
---

# Android Java → Unity C#：禁止 UnitySendMessage

## 硬约束

凡 **Java / Kotlin（AAR）回调 Unity C#**：

- **禁止**使用 `UnityPlayer.UnitySendMessage`（含 `UnitySendMessage` 字符串反射调 MonoBehaviour）。
- **必须**使用 **JNI 回调接口**，首选 **`AndroidJavaProxy`**（C# 实现 Java 接口，注册给 AAR）。

`UnitySendMessage` 依赖 GameObject 名、方法名字符串、主线程派发，难测、易丢消息、与 IL2CPP 裁剪/重构不友好；本工程 **一律不用**。

## 推荐模式

### Java：定义回调接口

```java
// com.wangcq327.vrboardcast.slavedownload.IDownloadListener
public interface IDownloadListener {
    void onProgress(String taskId, long bytesReceived, long bytesTotal, long bytesPerSecond);
    void onStateChanged(String taskId, String state, String errorMessage);
}
```

### C#：AndroidJavaProxy 实现并传入 Java

```csharp
public sealed class SlaveDownloadListenerProxy : AndroidJavaProxy
{
    public SlaveDownloadListenerProxy() : base("com.wangcq327.vrboardcast.slavedownload.IDownloadListener") { }

    void onProgress(string taskId, long bytesReceived, long bytesTotal, long bytesPerSecond) { /* 主线程派发 */ }
    void onStateChanged(string taskId, string state, string errorMessage) { /* ... */ }
}

// 启动时：manager.Call("setListener", new SlaveDownloadListenerProxy());
```

### 主线程

Java 回调可能在 **非 Unity 主线程**（如 OkHttp 线程）。在 Proxy 内 **不要**直接调 Unity API；用 `UnityMainThreadDispatcher` / 已有主线程队列 **Enqueue** 后再更新状态。

## C# → Java

仍用 `AndroidJavaObject` / `AndroidJavaClass.Call`；与 Java→C# 方向独立，不受本节禁止项影响。

## 允许的其他 Java→C# 方式

| 方式 | 适用 |
|------|------|
| `AndroidJavaProxy` 实现 Java 接口 | **首选**，进度/状态/完成 |
| C# **主动轮询** Java `getProgress(taskId)` | 被动 Query、低频快照 |
| 共享内存 / 文件 / Socket | **禁止**为本项目片源同步首选；除非有书面设计例外 |

## 相关

- 片源下载 AAR：[docs/Slave片源同步-Android-AAR实现.md](../docs/Slave片源同步-Android-AAR实现.md) §5.2
- AAR 同步纪律：[aar-sync-unity.mdc](./aar-sync-unity.mdc)
