---
description: 每次任务收尾须说明 AAR/APK/PC 重编与 Unity 侧用户操作（VRBoardCast）
alwaysApply: true
---

# 收尾：构建、部署与 Unity 操作提示

完成**任意代码/资源/场景/协议**相关改动后，回复**末尾必须**增加一节（无涉及则写「无额外构建」）：

## 固定标题

使用二级标题：**## 你需要做的（构建与 Unity）**

## 必须写清的内容

按本次 diff **逐项勾选**，只写**本次实际涉及**的项，不要套模板空话：

| 检查项 | 何时要写 | 写给用户的内容要点 |
|--------|----------|-------------------|
| **Android AAR** | 改了 `Plugins/Android/*.aar`、Gradle 子工程、`userpackages/**`、JNI/Manifest/原生资源 | 在哪个目录执行什么命令、`.aar` 覆盖到哪；详见 `aar-sync-unity.mdc` |
| **Slave APK** | 改了会进头显的逻辑：`Assets/Broadcast/Slave/**`、`Broadcast.Core/Protocol/**`、Slave 场景、`Plugins/Android`（经 AAR 进 APK）、XR/Pico/AVPro Android 相关 | **须重新打 Android 包**；说明场景（如 `Android`）、`BroadcastRoleConfig` 为 Slave |
| **PC Master 可执行程序** | 改了 `Assets/Broadcast/Master/**`、`WebControl/**`、`StreamingAssets/web/**`、Master 场景 | **须重新 Build Windows（或你们用的 Master 目标平台）**；若只改 Web 静态页，注明是否需重打 EXE 还是仅覆盖 `StreamingAssets/web` |
| **仅文档 / 仅 Cursor 规则** | 只动 `docs/**`、`.cursor/**` | 写明 **不必** AAR/APK/Unity 构建 |
| **Unity 编辑器内操作** | 新增组件、序列化字段、场景引用、Prefab、Inspector 绑定、`SerializeField` 拖拽 | 列出用户要在 Unity 里**打开的场景**、**挂到哪个物体**、**Inspector 要拖哪些引用**、是否 Play 模式验证 |
| **版本配套** | Master/Slave 协议或 DTO 变更 | 写明 **Master 与 Slave 须同版本**，只升一端会怎样 |

## 书写要求

- 用**简体中文**，短列表，每条一行，可执行（「去 Unity → File → Build Settings → Android → Build」级别）。
- **不要**在「无涉及」时省略整节：写「无额外构建；Unity 无需操作。」
- AAR 细则不重复长文，写「见 `aar-sync-unity` 规则」并给出**本次**具体路径/命令即可。
- 助手**不能代替用户在 Unity 里点 Build**时，必须明确写「请你本地执行」。

## 示例（仅 C# Slave + Master 协议）

```markdown
## 你需要做的（构建与 Unity）

- **AAR**：不必（未改 `Plugins/Android` / Gradle）。
- **Slave APK**：须重打；场景 `Assets/Scenes/Android.unity`，角色为 Slave。
- **PC Master**：须重打 Windows 包（`Broadcast.Master` / 协议变更）。
- **Unity 编辑器**：若新加了 `[SerializeField]`，请在 Master 场景把 `xxx` 拖到 `yyy`；无则写「无 Inspector 变更」。
- **配套**：Master 与 Slave 须同时更新，否则旧 Slave 不识别新消息。
```
