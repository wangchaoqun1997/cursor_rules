---
description: VRBoardCast 影院播控 Unity 工程背景、文档与开发约定（Pico4×80 / PC 中控）
alwaysApply: true
---

# VRBoardCast 工程约定

本仓库为 **VR 影院播控系统**（Unity），与前项目共用 Cursor 工作流（Git 中文提交、Android AAR 同步纪律见同目录其他规则）。

## 项目一句话

一台 **Windows PC 中控** 无线指挥约 **80 台 Pico 4**，统一播 **4K 3D 180°** 片源；**声音只从影院 HDMI 音响出**，VR **静音**跟画；画面与现场音响对齐 **≤100ms**，持续纠偏。

## 必读文档（改需求/架构前先查）

| 文档 | 路径 |
|------|------|
| 需求与方案（文首有大白话） | `docs/VR播控系统-需求与方案.md` |
| Unity 跨平台实现与插件 | `docs/Unity实现指南-跨平台插件与架构.md` |
| 局域网与 AP | `docs/局域网与AP组网说明.md` |

## 代码架构（新增代码须遵守）

- **面向对象与类规模**：遵循 OOP（SRP、接口隔离、组合）；**单类建议 ≤300 行，超过 500 行必须拆分**，禁止无故上千行的上帝类。详见 `.cursor/rules/code-design-oop.mdc`。
- **单工程多构建**：同一 Unity 工程；场景内 `BroadcastRoleConfig` 选 **Master**（PC 中控）或 **Slave**（Pico 头显），不再用 `ROLE_MASTER` / `ROLE_SLAVE` 宏。
- **Core 不引用** Pico SDK / XR；平台差异走 `Platform/` 接口 + `#if`。
- **网络**：LiteNetLib（Reliable 控制通道 + Unreliable 20Hz Sync）；**视频**：优先 AVPro（Win+Android 同一套 API）。
- **片源（首期）**：VR 本地文件 + U 盘/manifest 拷贝；不做实时推流；中控推送为后期。
- **Slave 外存路径**：Android 真机**禁止** `Application.persistentDataPath`（`/data/user/0/...` 与 adb 不一致）；统一经 `AndroidAppStoragePaths`（JNI `getExternalFilesDir`）→ `SlaveContentPathResolver`；禁止硬编码 `/storage/...`、`/sdcard/Android/data/...` 写入。详见 `.cursor/rules/android-slave-storage-paths.mdc`。

## 改动范围

- 优先 **最小 diff**，不扩无关功能。
- 注释只写非显而易见逻辑；代码与现有风格一致。
- 与用户沟通使用 **简体中文**。

## 测试与样片

- 4K 180 转码参数、Pico 硬解上限：**样片待客户提供**后再锁 profile；勿在未实测前写死码率。
