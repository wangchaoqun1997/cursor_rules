---
description: Android 外存路径禁止硬编码，须经 JNI getExternalFilesDir（若本工程有外存读写）
globs: aarForAllSlaves/**/*.cs,Assets/**/*.cs
alwaysApply: false
---

# Android 外存路径（禁止硬编码）

## 硬约束

在 **头显（Android）** 读写应用外存时：

- **禁止**在 C# 里手写或拼接 `/storage/emulated/0/Android/data/<包名>/...`、`/sdcard/Android/data/...` 用于 **写入**。
- **禁止**在 Android 真机上依赖 `Application.persistentDataPath` 与运维 adb 路径混用（二者常不一致）。
- **必须**经 JNI `Context.getExternalFilesDir`（或工程内已有统一封装）取得可写根路径。

Pico / Android 11+ scoped storage 下，硬编码外存路径常报 `Access denied`。

## 说明

本规则随工程需要启用；当前主路径为 **VR局域网控**（Master / gameslauncher / 业务 UPM），若新增外存配置读写，优先复用已有路径封装，勿从旧「VRBoardCast 播控」工程照搬硬编码。
